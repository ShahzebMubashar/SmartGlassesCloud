# YOLO pipeline (yolo.py) + demo (demo/demo.py)
#
# easyocr depends on opencv-python-headless, which breaks cv2.imshow on Windows.
# After install: pip uninstall opencv-python-headless -y
# Then: pip install opencv-python
#
ultralytics>=8.0.0
opencv-python>=4.8.0
Pillow>=10.0.0
transformers>=4.36.0
torch>=2.0.0
torchvision>=0.15.0
pytesseract>=0.3.10
SpeechRecognition>=3.10.0
easyocr>=1.7.0
gradio
gTTS
opencv-python-headless
numpy
